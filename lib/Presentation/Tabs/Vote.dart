import 'package:flutter/material.dart';

class Vote extends StatelessWidget {
  const Vote ({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child:Text('ГОЛОСОВАНИЯ'));
  }
}